logging:
    level:
        com:
            marketGola:
                user:
                    repository:
                        mybatis: trace
mybatis:
    configuration:
        map-underscore-to-camel-case: true
    mapper-locations: classpath:/mapper/**/*.xml
    type-aliases-package: com.marketGola.User.domain
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password:
        url:
        username:
    profiles:
        active: test
